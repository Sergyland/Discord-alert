{
  "name": "discord-action",
  "version": "0.1.0",
  "description": "A github action that sends messages on discord.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Serge GEORGE",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.7",
    "@actions/github": "^5.0.0",
    "discord.js": "^12.5.3"
  },
  "devDependencies": {
    "dotenv": "^9.0.2"
  }
}
