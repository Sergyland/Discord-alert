{
  "name": "discord-action",
  "version": "0.1.0",
  "description": "A github action that sends messages on discord.",
  "main": "src/index.js",
  "scripts": {
    "start": "node ./src/index",
    "dev": "node ./src/dev",
    "package": "ncc build --source-map --license licenses.txt --external dotenv --stats-out build_stats.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Serge GEORGE",
  "license": "MIT",
  "dependencies": {
    "@actions/github": "^5.0.0",
    "discord.js": "^12.5.3",
    "dotenv": "^9.0.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.28.5"
  }
}